# uncomment the following line, if needed, to install the {usethis} package
# install.packages('usethis')

library(usethis)
use_git_config(user.name = "NicoJaws23", user.email = "nico.jaworski@utexas.edu")
detach(package:usethis)
